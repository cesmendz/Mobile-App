 <VerticalStackLayout
     Padding="30,0"
     Spacing="10"
     HorizontalOptions="Center"
     VerticalOptions="Center" >
     <Image
         Source="s.png"
         HeightRequest="950"
         Aspect="AspectFit"
             HorizontalOptions="Center"
             VerticalOptions="Center" />

 </VerticalStackLayout>
